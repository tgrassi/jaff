---
tags:
    - Api
    - Code-generation
---

# get_language_tokens

`#!python Codegen.get_language_tokens()`

Returns the full language token dictionary mapping normalized language names to their code generation configuration (brackets, offsets, comment style, etc.).

**Returns**

_dict[str, LangModifier]_
: Mapping from language id to language configuration object.
