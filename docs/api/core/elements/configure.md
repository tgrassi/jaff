---
tags:
    - Api
    - Elements
---

# configure

`#!python configure(mass_dict)`

Class-level method. Overrides the mass dictionary for both `Elements` and `Element` (propagates to `Element.configure`).

**Parameters**

**mass_dict** : _dict_
: Element mass dictionary.
